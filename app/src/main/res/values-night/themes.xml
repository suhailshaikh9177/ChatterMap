<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme for Dark Mode. -->
    <style name="Theme.LocatorChat" parent="Theme.Material3.Dark.NoActionBar">
        <item name="colorPrimary">@color/dark_ui_accent</item>
        <item name="colorOnPrimary">@color/dark_ui_text_primary</item>
        <!-- MODIFIED: Changed from drawable to a solid color to prevent crash -->
        <item name="android:colorBackground">@color/dark_ui_background</item>
        <item name="colorSurface">@color/dark_ui_background</item>
        <item name="colorOnBackground">@color/dark_ui_text_primary</item>
        <item name="colorOnSurface">@color/dark_ui_text_primary</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
    </style>
</resources>